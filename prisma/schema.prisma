generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Submission {
  id              String  @id
  name            String
  text            String
  isHadir         Boolean
  jumlahKehadiran Int
}
